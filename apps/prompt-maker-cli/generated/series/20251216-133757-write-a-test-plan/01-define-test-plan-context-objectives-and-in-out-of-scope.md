Create the opening sections of a Test Plan for <SYSTEM/PRODUCT>.

Include:
1) Document purpose and audience.
2) Test objectives (3–8 bullet points) tied to business/quality goals.
3) In-scope items (features, components, platforms) and out-of-scope items.
4) Assumptions and dependencies (explicitly list unknowns).
5) References (links or identifiers for PRD, user stories, architecture docs, APIs, compliance requirements).

Constraints:
- Use placeholders like <PRD_LINK> if real links are not available.
- Keep scope statements testable (avoid vague wording).

Validation:
- A reviewer can point to a clearly labeled Purpose, Objectives, In Scope, Out of Scope, Assumptions/Dependencies, and References section.
- Each in-scope item is specific enough to be mapped to at least one test area (e.g., feature/module/platform).
- At least 3 assumptions/dependencies are listed if inputs are incomplete; otherwise, all are grounded in provided inputs.